--- a/src/utils/libfabric/libfabric_rail.cpp
+++ b/src/utils/libfabric/libfabric_rail.cpp
@@ -411,7 +411,8 @@ nixlLibfabricRail::nixlLibfabricRail(
         throw std::runtime_error("Failed to allocate fi_info for rail " + std::to_string(rail_id));
     }
     hints->caps = 0;
-    hints->caps = FI_MSG | FI_RMA | FI_HMEM; // Try with FI_HMEM first
+    // Two-sided messaging for EFA compatibility (remove FI_RMA dependency)
+    hints->caps = FI_MSG | FI_HMEM; // Two-sided only, try with FI_HMEM first
     hints->caps |= FI_LOCAL_COMM | FI_REMOTE_COMM;
     hints->mode = FI_CONTEXT;
     hints->ep_attr->type = FI_EP_RDM;
@@ -443,7 +444,8 @@ nixlLibfabricRail::nixlLibfabricRail(
                       << ", retrying without FI_HMEM";

             // Retry without FI_HMEM
-            hints->caps = FI_MSG | FI_RMA;
+            // Two-sided messaging only
+            hints->caps = FI_MSG;
             hints->caps |= FI_LOCAL_COMM | FI_REMOTE_COMM;

             ret = fi_getinfo(FI_VERSION(1, 18), NULL, NULL, 0, hints, &info);
@@ -1092,42 +1094,44 @@ nixlLibfabricRail::postWrite(const void *local_buffer,
     NIXL_TRACE << "Posting RDMA write on endpoint=" << endpoint << " local_buffer=" << local_buffer
                << " length=" << length << " immediate_data=" << immediate_data
                << " dest_addr=" << dest_addr << " remote_addr=" << (void *)remote_addr
-               << " remote_key=" << remote_key << " context=" << &req->ctx;
+               << " context=" << &req->ctx;

-    // Retry indefinitely until writedata succeeds or fails for all providers
+    // Two-sided send (replaces fi_writedata with fi_senddata)
+    // Use fi_senddata to preserve immediate_data functionality
     int ret = -FI_EAGAIN;
     int attempt = 0;

     while (true) {
-        // Libfabric fi_writedata call
-        ret = fi_writedata(endpoint,
-                           local_buffer,
-                           length,
-                           local_desc,
-                           immediate_data,
-                           dest_addr,
-                           remote_addr,
-                           remote_key,
-                           &req->ctx);
+        // Libfabric fi_senddata call (two-sided messaging)
+        // Note: remote_addr and remote_key are ignored in two-sided mode
+        ret = fi_senddata(endpoint,
+                         local_buffer,
+                         length,
+                         local_desc,
+                         immediate_data,
+                         dest_addr,
+                         &req->ctx);

         if (ret == 0) {
             // Success
-            NIXL_TRACE << "RDMA write posted successfully"
+            NIXL_TRACE << "fi_senddata posted successfully (two-sided)"
                        << (attempt > 0 ? " after " + std::to_string(attempt + 1) + " attempts" :
                                          "");
             return NIXL_SUCCESS;
         }

         if (ret == -FI_EAGAIN) {
-            // Resource temporarily unavailable - retry indefinitely for all providers
+            // Resource temporarily unavailable - retry
             attempt++;

             // Log every N attempts to avoid log spam
             if (attempt % NIXL_LIBFABRIC_LOG_INTERVAL_ATTEMPTS == 0) {
-                NIXL_INFO << "fi_writedata still retrying EAGAIN on rail " << rail_id << " after "
+                NIXL_INFO << "fi_senddata still retrying EAGAIN on rail " << rail_id << " after "
                           << attempt << " attempts";
             } else {
-                NIXL_TRACE << "fi_writedata returned EAGAIN on rail " << rail_id
+                NIXL_TRACE << "fi_senddata returned EAGAIN on rail " << rail_id
                            << ", retrying (attempt " << attempt << ")";
             }

@@ -1144,7 +1148,7 @@ nixlLibfabricRail::postWrite(const void *local_buffer,
         }
     }

-    NIXL_ERROR << "fi_writedata failed on rail " << rail_id << ": " << fi_strerror(-ret);
+    NIXL_ERROR << "fi_senddata failed on rail " << rail_id << ": " << fi_strerror(-ret);
     return NIXL_ERR_BACKEND;
 }

@@ -1165,39 +1169,41 @@ nixlLibfabricRail::postRead(void *local_buffer,
     NIXL_TRACE << "Posting RDMA read on endpoint=" << std::hex << endpoint
                << " local_buffer=" << local_buffer << " length=" << length
                << " dest_addr=" << dest_addr << " remote_addr=" << (void *)remote_addr
-               << " remote_key=" << remote_key << " context=" << &req->ctx;
+               << " context=" << &req->ctx;

-    // Retry indefinitely until readdata succeeds or fails for all providers
+    // Two-sided receive (replaces fi_read with fi_recv)
     int ret = -FI_EAGAIN;
     int attempt = 0;

     while (true) {
-        // Libfabric fi_read call
-        ret = fi_read(endpoint,
-                      local_buffer,
-                      length,
-                      local_desc,
-                      dest_addr,
-                      remote_addr,
-                      remote_key,
-                      &req->ctx);
+        // Libfabric fi_recv call (two-sided messaging)
+        // Note: remote_addr and remote_key are ignored in two-sided mode
+        ret = fi_recv(endpoint,
+                     local_buffer,
+                     length,
+                     local_desc,
+                     dest_addr,
+                     &req->ctx);

         if (ret == 0) {
             // Success
-            NIXL_TRACE << "RDMA read posted successfully"
+            NIXL_TRACE << "fi_recv posted successfully (two-sided)"
                        << (attempt > 0 ? " after " + std::to_string(attempt + 1) + " attempts" :
                                          "");
             return NIXL_SUCCESS;
         }

         if (ret == -FI_EAGAIN) {
-            // Resource temporarily unavailable - retry indefinitely for all providers
+            // Resource temporarily unavailable - retry
             attempt++;

             // Log every N attempts to avoid log spam
             if (attempt % NIXL_LIBFABRIC_LOG_INTERVAL_ATTEMPTS == 0) {
-                NIXL_INFO << "fi_read still retrying EAGAIN on rail " << rail_id << " after "
+                NIXL_INFO << "fi_recv still retrying EAGAIN on rail " << rail_id << " after "
                           << attempt << " attempts";
             } else {
-                NIXL_TRACE << "fi_read returned EAGAIN on rail " << rail_id
+                NIXL_TRACE << "fi_recv returned EAGAIN on rail " << rail_id
                            << ", retrying (attempt " << attempt << ")";
             }

@@ -1216,7 +1222,7 @@ nixlLibfabricRail::postRead(void *local_buffer,
         }
     }

-    NIXL_ERROR << "fi_read failed on rail " << rail_id << ": " << fi_strerror(-ret);
+    NIXL_ERROR << "fi_recv failed on rail " << rail_id << ": " << fi_strerror(-ret);
     return NIXL_ERR_BACKEND;
 }

@@ -1244,7 +1250,9 @@ nixlLibfabricRail::registerMemory(void *buffer,

     // Determine access flags based on provider capabilities
     uint64_t provider_access_flags;
-    if (provider_name == "tcp" || provider_name == "sockets") {
+    // Two-sided messaging: use FI_SEND | FI_RECV for all providers
+    provider_access_flags = FI_SEND | FI_RECV;
+    if (false && (provider_name == "tcp" || provider_name == "sockets")) {
         // TCP provider only needs basic read/write access
         provider_access_flags = FI_READ | FI_WRITE;
     } else {
